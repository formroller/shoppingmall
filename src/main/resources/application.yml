spring:
  datasource:
#    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/practiceDB?createDatabaseIfNotExist=true
    user: practice
    password: test

  jpa:
    hibernate:
#      ddlAuto: create
      ddlAuto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        show_sql: true
        format_sql: true


level:
  logging:
    org:
      springframework:
        security:
          debug: trace


